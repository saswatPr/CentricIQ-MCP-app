{
  "name": "centriciq-mcp-app-v2",
  "version": "2.0.0",
  "description": "CentricIQ MCP Apps server — raw Streamable HTTP + JSON-RPC, zero dependencies. Rebuilt on a verified-working base after the @modelcontextprotocol/ext-apps SDK path failed to render in claude.ai for a custom connector.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
